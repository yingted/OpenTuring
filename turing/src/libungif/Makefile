### Generated by Winemaker 0.8.3
###
### Invocation command line was
### /usr/bin/winemaker .


SRCDIR                = .
DLLS                  =
LIBS                  = liblibungif.a
EXES                  =



### Common settings

CEXTRA                = -mno-cygwin
CXXEXTRA              = -mno-cygwin
RCEXTRA               =
DEFINES               =
INCLUDE_PATH          = -I.
DLL_PATH              =
DLL_IMPORTS           =
LIBRARY_PATH          =
LIBRARIES             =


### liblibungif.a sources and settings

liblibungif_a_MODULE  = liblibungif.a
liblibungif_a_C_SRCS  = dev2gif.c \
			dgif_lib.c \
			egif_lib.c \
			getarg.c \
			gif_err.c \
			gif_font.c \
			gifalloc.c \
			qprintf.c \
			quantize.c
liblibungif_a_CXX_SRCS=
liblibungif_a_RC_SRCS =
liblibungif_a_LDFLAGS = -mno-cygwin
liblibungif_a_ARFLAGS = rc
liblibungif_a_DLL_PATH=
liblibungif_a_DLLS    = odbc32 \
			ole32 \
			oleaut32 \
			winspool \
			odbccp32
liblibungif_a_LIBRARY_PATH=
liblibungif_a_LIBRARIES= uuid

liblibungif_a_OBJS    = $(liblibungif_a_C_SRCS:.c=.o) \
			$(liblibungif_a_CXX_SRCS:.cpp=.o) \
			$(liblibungif_a_RC_SRCS:.rc=.res)



### Global source lists

C_SRCS                = $(liblibungif_a_C_SRCS)
CXX_SRCS              = $(liblibungif_a_CXX_SRCS)
RC_SRCS               = $(liblibungif_a_RC_SRCS)


### Tools

CC = winegcc
CXX = wineg++
RC = wrc
AR = ar


### Generic targets

all: $(DLLS:%=%.so) $(LIBS) $(EXES)

### Build rules

.PHONY: all clean dummy

$(SUBDIRS): dummy
	@cd $@ && $(MAKE)

# Implicit rules

.SUFFIXES: .cpp .cxx .rc .res
DEFINCL = $(INCLUDE_PATH) $(DEFINES) $(OPTIONS)

.c.o:
	$(CC) -c $(CFLAGS) $(CEXTRA) $(DEFINCL) -o $@ $<

.cpp.o:
	$(CXX) -c $(CXXFLAGS) $(CXXEXTRA) $(DEFINCL) -o $@ $<

.cxx.o:
	$(CXX) -c $(CXXFLAGS) $(CXXEXTRA) $(DEFINCL) -o $@ $<

.rc.res:
	$(RC) $(RCFLAGS) $(RCEXTRA) $(DEFINCL) -fo$@ $<

# Rules for cleaning

CLEAN_FILES     = y.tab.c y.tab.h lex.yy.c core *.orig *.rej \
                  \\\#*\\\# *~ *% .\\\#*

clean:: $(SUBDIRS:%=%/__clean__) $(EXTRASUBDIRS:%=%/__clean__)
	$(RM) $(CLEAN_FILES) $(RC_SRCS:.rc=.res) $(C_SRCS:.c=.o) $(CXX_SRCS:.cpp=.o)
	$(RM) $(DLLS:%=%.so) $(LIBS) $(EXES) $(EXES:%=%.so)

$(SUBDIRS:%=%/__clean__): dummy
	cd `dirname $@` && $(MAKE) clean

$(EXTRASUBDIRS:%=%/__clean__): dummy
	-cd `dirname $@` && $(RM) $(CLEAN_FILES)

### Target specific build rules
DEFLIB = $(LIBRARY_PATH) $(LIBRARIES) $(DLL_PATH) $(DLL_IMPORTS:%=-l%)

$(liblibungif_a_MODULE): $(liblibungif_a_OBJS)
	$(AR) $(liblibungif_a_ARFLAGS) $@ $(liblibungif_a_OBJS)


